# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json

# Tooling de desenvolvimento local — não pertence ao repo do app
# (.claude/ tem settings.local.json com allowlist/segredos + skills instaladas
#  como a 'impeccable'; DESIGN.md/PRODUCT.md são artefatos gerados por elas)
.claude/
.impeccable/
.github/skills/
DESIGN.md
PRODUCT.md

# MCP servers config (contém tokens de acesso)
.mcp.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Bundle analysis
bundle-report.html
stats.json

# Supabase CLI local cache / linked project metadata
supabase/.temp/
